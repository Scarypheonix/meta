// Generated by tests/web/picture_test.go. Do not edit.
//
// The tutorial's programs, as text, so that the page's copy and run buttons hand
// over exactly what web/pictures/*.svg shows.
//
// Regenerate: UPDATE_GOLDEN=1 go test ./tests/web/
window.originTutorial = {
  "01-hello": "println(\"Hello, Origin.\")\n",
  "02-ask": "let name = ask(\"What is your name? \")\nprintln(\"Hello, \\(name)!\")\n",
  "03-numbers": "let answer = ask(\"How many slices? \")\n\nif let Some(n) = answer.parse_int() {\n    println(\"That is \\(n * 2) halves.\")\n} else {\n    println(\"I could not read that as a number.\")\n}\n",
  "04-choices": "let score = 87\n\nif score >= 90 {\n    println(\"top marks\")\n} else if score >= 60 {\n    println(\"a pass\")\n} else {\n    println(\"not this time\")\n}\n\n// A match is the same idea when there is a fixed set of answers.\nlet day = \"sat\"\nmatch day {\n    \"sat\" => println(\"weekend\"),\n    \"sun\" => println(\"weekend\"),\n    other => println(\"\\(other) is a school day\"),\n}\n",
  "05-repeating": "for n in range(1, 4) {\n    println(\"\\(n)...\")\n}\nprintln(\"go\")\n\nlet mut left = 3\nwhile left > 0 {\n    println(\"\\(left) to go\")\n    left = left - 1\n}\n",
  "06-lists": "let names = [\"ada\", \"grace\", \"alan\", \"edsger\"]\n\nprintln(\"there are \\(names.len()) of them\")\nprintln(names.join(\", \"))\n\nlet long = names.filter(|n| n.len() > 4)\nprintln(\"longer than four letters: \\(long.join(\", \"))\")\n\nfor n in range(1, 4) {\n    println(\"\\(n) squared is \\(n * n)\")\n}\n",
  "07-functions": "fn fib(n: i64) -> i64 {\n    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }\n}\n\nfor i in range(0, 11) {\n    println(fib(i).to_str())\n}\n",
  "08-your-own-types": "struct Dog {\n    name: String,\n    mut treats: i64,\n}\n\nfn feed(d: Dog) {\n    d.treats = d.treats + 1\n}\n\nlet rex = Dog { name: \"Rex\", treats: 0 };\nfeed(rex)\nfeed(rex)\nprintln(\"\\(rex.name) has had \\(rex.treats) treats\")\n",
  "09-nothing-and-errors": "// There is no null in Origin. A thing that might not be there is an Option,\n// and the compiler will not let you forget to check.\n\nlet names = [\"ada\", \"grace\"]\n\nmatch names.first() {\n    Some(n) => println(\"first: \\(n)\"),\n    None => println(\"the list is empty\"),\n}\n\n// And a thing that might go wrong is a Result, which is a value too.\nmatch read_to_string(\"notes.txt\") {\n    Ok(text) => println(\"\\(text.len()) bytes\"),\n    Err(e) => println(\"could not read it: \\(e.to_str())\"),\n}\n",
};
