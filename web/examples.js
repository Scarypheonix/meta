// Generated by tests/web/examples_test.go. Do not edit.
//
// The worked examples of docs/spec/10-examples.md, taken from the end-to-end cases
// derived from it, so that every program the playground offers is one the suite
// already runs on three engines at three optimization levels.
//
// Regenerate: UPDATE_GOLDEN=1 go test ./tests/web/
window.originExamples = [
  { title: "Hello", name: "hello", source: "println(\"Hello, Origin.\")\n", stdin: "" },
  { title: "Ask a question", name: "ask_a_question", source: "// Standard input, a line at a time (spec/18-input.md, ADR-0043).\n//\n// `ask` prints a prompt and reads one line. `input` reads a line with no prompt, and\n// `read_line` is the honest one: `Option[String]`, so `None` is the end of the input and\n// an empty line is `Some(\"\")`.\n//\n// In the playground there is no terminal to type into, so the box below the editor is\n// standard input: whatever is in it, this program reads.\n\nlet name = ask(\"What is your name? \")\nprintln(\"Hello, \\(name)!\")\n\nlet answer = ask(\"How old are you? \")\nif let Some(age) = answer.parse_int() {\n    println(\"In ten years you will be \\(age + 10).\")\n} else {\n    println(\"`\\(answer)` is not a number, but that is all right.\")\n}\n", stdin: "Ada\n36\n" },
  { title: "Lists and loops", name: "lists_and_loops", source: "// Lists, and the methods on them (spec/13-collections.md).\n//\n// `[a, b, c]` builds a `List`; `range(lo, hi)` counts; and the transforms are ordinary\n// Origin in the prelude rather than anything the compiler knows about.\n\nlet names = [\"ada\", \"grace\", \"alan\", \"edsger\"]\n\nprintln(\"there are \\(names.len()) of them\")\nprintln(names.join(\", \"))\n\nlet long = names.filter(|n| n.len() > 4)\nprintln(\"longer than four letters: \\(long.join(\", \"))\")\n\nlet lengths = names.map(|n| n.len())\nprintln(\"letters in total: \\(lengths.sum())\")\n\nfor n in range(1, 6) {\n    println(\"\\(n) squared is \\(n * n)\")\n}\n", stdin: "" },
  { title: "Recursive fibonacci", name: "fib", source: "fn fib(n: i64) -> i64 {\n    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }\n}\n\nfor i in range(0, 11) {\n    println(fib(i).to_str())\n}\n", stdin: "" },
  { title: "Closure counter", name: "closure_counter", source: "struct Cell { mut value: i64 }\n\nfn make_counter() -> fn() -> i64 {\n    // A `let` whose value ends in `}` keeps its semicolon: `}` is not a statement\n    // terminator, because a block's last expression is its value (ADR-0040).\n    let cell = Cell { value: 0 };\n    || { cell.value = cell.value + 1; cell.value }\n}\n\nlet c = make_counter()\nlet d = make_counter()\nprintln(c().to_str())\nprintln(c().to_str())\nprintln(d().to_str())\n", stdin: "" },
  { title: "Recursive enum: a linked list", name: "linked_list", source: "enum Chain { Nil, Cons(i64, Chain) }\n\nfn total(c: Chain) -> i64 {\n    match c {\n        Chain::Nil => 0,\n        Chain::Cons(head, tail) => head + total(tail),\n    }\n}\n\nlet c = Chain::Cons(1, Chain::Cons(2, Chain::Cons(3, Chain::Nil)))\nprintln(total(c).to_str())\n", stdin: "" },
  { title: "Mutual recursion", name: "mutual_recursion", source: "fn is_even(n: i64) -> bool { if n == 0 { true } else { is_odd(n - 1) } }\nfn is_odd(n: i64) -> bool { if n == 0 { false } else { is_even(n - 1) } }\n\nprintln(is_even(10).to_str())\nprintln(is_odd(10).to_str())\n", stdin: "" },
  { title: "Generics with a trait bound", name: "generics_ord", source: "fn max2[T: Ord](a: T, b: T) -> T {\n    match a.cmp(b) {\n        Less => b,\n        Equal => a,\n        Greater => a,\n    }\n}\n\nprintln(max2(3, 7).to_str())\nprintln(max2(\"apple\", \"banana\"))\n", stdin: "" },
  { title: "Integer overflow traps", name: "overflow_traps", source: "let x = 9223372036854775807\nprintln(\"before\")\nlet y = x + 1\nprintln(y.to_str())\n", stdin: "" },
  { title: "Explicit wrapping", name: "wrapping_and_casts", source: "let x = 9223372036854775807\nprintln(x.wrapping_add(1).to_str())\nprintln((300 as u8).to_str())\nprintln(((-7) / 2).to_str())\nprintln(((-7) % 2).to_str())\n", stdin: "" },
  { title: "Result and ?", name: "result_and_try", source: "enum ParseError { Empty, BadDigit(char) }\n\nfn parse_digit(c: char) -> Result[i64, ParseError] {\n    if c >= '0' && c <= '9' {\n        Ok(c as u32 as i64 - 48)\n    } else {\n        Err(ParseError::BadDigit(c))\n    }\n}\n\nfn parse_two(a: char, b: char) -> Result[i64, ParseError] {\n    // `?` ends a statement, so neither of these needs a semicolon (ADR-0040, amended in\n    // Phase 15). The last line has none either, and that is what makes it the value.\n    let x = parse_digit(a)?\n    let y = parse_digit(b)?\n    Ok(x * 10 + y)\n}\n\nmatch parse_two('4', '2') {\n    Ok(n) => println(n.to_str()),\n    Err(_) => println(\"error\"),\n}\nmatch parse_two('4', 'z') {\n    Ok(n) => println(n.to_str()),\n    Err(ParseError::BadDigit(_)) => println(\"bad digit\"),\n    Err(ParseError::Empty) => println(\"empty\"),\n}\n", stdin: "" },
  { title: "Mutation through an alias", name: "aliasing", source: "struct Counter { mut n: i64 }\n\nfn bump(c: Counter) { c.n = c.n + 1 }\n\nlet a = Counter { n: 0 };\nlet b = a\nbump(b)\nbump(a)\nprintln(a.n.to_str())\nprintln(ref_eq(a, b).to_str())\nprintln((Counter { n: 2 } == a).to_str())\nprintln(ref_eq(Counter { n: 2 }, a).to_str())\n", stdin: "" },
  { title: "for over an iterator", name: "for_loop", source: "struct Counter { mut n: i64 }\n\nimpl IntoIterator for Counter {\n    type Item = i64\n    type Iter = Counter\n    fn into_iter(self) -> Counter { self }\n}\n\nimpl Iterator for Counter {\n    type Item = i64\n    fn next(mut self) -> Option[i64] {\n        if self.n > 0 {\n            self.n = self.n - 1\n            Some(self.n)\n        } else {\n            None\n        }\n    }\n}\n\nlet mut total = 0\nfor v in (Counter { n: 5 }) {\n    total = total + v\n}\nprintln(total.to_str())\n\nlet mut count = 0\nfor v in (Counter { n: 10 }) {\n    if v == 6 {\n        break\n    }\n    count = count + 1\n}\nprintln(count.to_str())\n\nlet mut odds = 0\nfor v in (Counter { n: 6 }) {\n    if v % 2 == 0 {\n        continue\n    }\n    odds = odds + 1\n}\nprintln(odds.to_str())\n\nlet mut empty_runs = 0\nfor v in (Counter { n: 0 }) {\n    empty_runs = empty_runs + v\n}\nprintln(empty_runs.to_str())\n\nfor v in (Counter { n: 3 }) {\n    println(v.to_str())\n}\n", stdin: "" },
  { title: "Green threads and channels", name: "thread_spawn_and_join", source: "use std::thread;\n\nlet h = thread::spawn(|| -> i64 { 40 + 2 })\nprintln(h.join().to_str())\n\n// Joining in a fixed order makes the output independent of scheduling, which is what\n// lets this be a differential case at all (spec/12-concurrency.md).\nlet a = thread::spawn(|| -> i64 { 1 })\nlet b = thread::spawn(|| -> i64 { 2 })\nlet c = thread::spawn(|| -> i64 { 3 })\nprintln((a.join() + b.join() + c.join()).to_str())\n", stdin: "" },
  { title: "A discarded allocation loop is reclaimed", name: "gc_reclaims_discarded_allocations", source: "struct Pair { a: i64, b: i64 }\n\nlet mut last = Pair { a: 0, b: 0 };\nlet mut i: i64 = 0\nwhile i < 5000000 {\n    last = Pair { a: i, b: i };\n    i = i + 1\n}\nprintln(last.a.to_str())\nprintln(last.b.to_str())\n", stdin: "" },
];
