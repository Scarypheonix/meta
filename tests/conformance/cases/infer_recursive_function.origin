// EXPECT: accept
// spec/07: an item is visible before it is declared, so recursion needs no forward declaration.

fn fib(n: i64) -> i64 {
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }
}

fn main() {
    let x = fib(10);
}
