// EXPECT: accept
// spec/05: String has no enumerable signature, so `_` closes the match.

fn f(s: String) -> i64 {
    match s {
        "yes" => 1,
        "no" => 0,
        _ => -1,
    }
}

fn main() { let x = f("yes"); }
