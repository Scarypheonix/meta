// EXPECT: accept
// spec/06: an impl makes the bound hold.

trait Special { fn mark(self) -> i64; }

struct Thing { }

impl Special for Thing {
    fn mark(self) -> i64 { 7 }
}

fn needs_special[T: Special](x: T) -> i64 { x.mark() }

fn main() {
    let n = needs_special(Thing { });
}
