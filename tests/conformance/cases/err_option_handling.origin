// EXPECT: accept
// spec/03: Option is an ordinary enum.

fn find(n: i64) -> Option[i64] {
    if n > 0 { Option::Some(n) } else { Option::None }
}

fn main() {
    let v = match find(1) {
        Option::Some(x) => x,
        Option::None => 0,
    };
}
