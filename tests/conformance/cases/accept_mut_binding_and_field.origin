// EXPECT: accept
// spec: 08-memory-model.md — `mut` on a binding permits reassignment; `mut` on a field
// permits mutation through any reference.

struct Counter { mut n: i64 }

fn main() {
    let mut i = 0;
    i = i + 1;
    let c = Counter { n: 0 };
    c.n = i;
}
