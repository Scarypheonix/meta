// EXPECT: accept
// spec/03: a named function is a function value.

fn double(n: i64) -> i64 { n * 2 }

fn apply(f: fn(i64) -> i64, x: i64) -> i64 { f(x) }

fn main() {
    let y = apply(double, 21);
}
