// EXPECT: accept
// spec/07: mutual recursion works for the same reason.

fn is_even(n: i64) -> bool { if n == 0 { true } else { is_odd(n - 1) } }
fn is_odd(n: i64) -> bool { if n == 0 { false } else { is_even(n - 1) } }

fn main() {
    let x = is_even(4);
}
