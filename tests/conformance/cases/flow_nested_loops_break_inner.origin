// EXPECT: accept
// spec/04: `break` binds to the innermost loop.

fn main() {
    let mut i = 0;
    while i < 3 {
        let mut j = 0;
        while j < 3 {
            if j == 1 { break; }
            j = j + 1;
        }
        i = i + 1;
    }
}
