fn classify(n: i64) -> i64 {
    if n < 0 {
        0 - 1
    } else {
        if n == 0 { 0 } else { 1 }
    }
}

fn main() {
    let mut i = 0;
    let mut total = 0;
    while i < 3 {
        total = total + classify(i);
        i = i + 1;
    }
}
