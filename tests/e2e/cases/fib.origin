use std::io;

fn fib(n: i64) -> i64 {
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }
}

fn main() {
    let mut i = 0;
    while i <= 10 {
        io::println(fib(i).to_str());
        i = i + 1;
    }
}
