struct Pair { a: i64, b: i64 }

let mut last = Pair { a: 0, b: 0 };
let mut i: i64 = 0
while i < 5000000 {
    last = Pair { a: i, b: i };
    i = i + 1
}
println(last.a.to_str())
println(last.b.to_str())
